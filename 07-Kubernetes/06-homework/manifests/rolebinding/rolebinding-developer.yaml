apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: developer-reader

subjects:

- kind: User
  name: developer

roleRef:

  apiGroup: rbac.authorization.k8s.io

  kind: Role

  name: pod-reader
