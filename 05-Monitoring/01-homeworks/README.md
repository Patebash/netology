# Домашнее задание к занятию "13.Системы мониторинга"

## Задание 1:
> Вас пригласили настроить мониторинг на проект. На онбординге вам рассказали, что проект представляет из себя платформу для вычислений с выдачей текстовых отчетов, которые сохраняются на диск. Взаимодействие с платформой осуществляется по протоколу http. Также вам отметили, что вычисления загружают ЦПУ. Какой минимальный набор метрик вы выведите в мониторинг и почему?

### Ответ:

Я бы начал с метрик по четырем золотым сигналам: трафик (RPS), задержка (latency), ошибки (5xx) и насыщение ресурсов (CPU, память, диск). Поскольку сервис выполняет вычисления и сохраняет результаты на диск, особое внимание уделил бы CPU utilization, load average, свободному месту на диске и времени генерации отчетов как ключевой бизнесовой метрике.

---

## Задание 2:

> Менеджер продукта посмотрев на ваши метрики сказал, что ему непонятно что такое RAM/inodes/CPUla. Также он сказал, что хочет понимать, насколько мы выполняем свои обязанности перед клиентами и какое качество обслуживания. Что вы можете ему предложить?

### Ответ:

Я бы предложил определить SLI (индикаторы качества сервиса), на их основе установить SLO (целевые показатели) и, при необходимости, SLA (обязательства перед клиентом). Например, отслеживать процент успешно сформированных отчетов, время генерации отчетов и доступность сервиса.

*Пример:*

* SLI: доля успешно сформированных отчетов, время формирования отчета, доступность API.
* SLO: 99% отчетов формируются менее чем за 10 секунд, доступность сервиса 99.9%.
* SLA: гарантируем клиентам доступность 99% в месяц.

---

## Задание 3:

> Вашей DevOps команде в этом году не выделили финансирование на построение системы сбора логов. Разработчики в свою очередь хотят видеть все ошибки, которые выдают их приложения. Какое решение вы можете предпринять в этой ситуации, чтобы разработчики получали ошибки приложения?

### Ответ:

Если нет бюджета на централизованный сбор логов, я бы вынес ошибки приложений в метрики, собирал их через Prometheus и строил дашборды и алерты в Grafana. Например, считать количество исключений по типам. Это позволит разработчикам видеть возникновение ошибок и их динамику даже без полноценной системы логирования. Для получения деталей ошибки можно хранить локальные логи или использовать бесплатный Sentry

---

## Задание 4:

>  Вы, как опытный SRE, сделали мониторинг, куда вывели отображения выполнения SLA=99% по http кодам ответов. Вычисляете этот параметр по следующей формуле: summ_2xx_requests/summ_all_requests. Данный параметр не поднимается выше 70%, но при этом в вашей системе нет кодов ответа 5xx и 4xx. Где у вас ошибка?

### Ответ:

Ошибка в формуле расчёта SLA. Я считаю успешными только ответы 2xx, но не учитываю 3xx. Если в системе много редиректов или ответов 304, они попадают в общее количество запросов, но не в числитель, из-за чего SLA получается около 70% даже при отсутствии 4xx и 5xx ошибок. Перед расчётом нужно корректно определить SLI и набор кодов, считающихся успешными.

*Корректная формула:*

SLI = (summ_2xx_requests + summ_3xx_requests) / (summ_all_requests)

---

## Задание 5:

>  Опишите основные плюсы и минусы pull и push систем мониторинга.

### Ответ:

#### Push-модель:

*Плюсы:*

* упрощение репликации данных в разные системы мониторинга или их резервные копии;
* более гибкая настройка отправки пакетов данных с метриками;
* UDP — это менее затратный способ передачи данных, из-за чего может возрасти производительность сбора метрик.

*Минусы:*

* возможна потеря метрик при сбое;
* нужна буферизация и ретраи;
* меньше контроля за тем, что реально доставлено.

#### Pull-модель:

*Плюсы:*

* легче контролировать подлинность данных;
* можно настроить единый proxy server до всех агентов с TLS;
* упрощённая отладка получения данных с агентов.

*Минусы:*

* сложнее доступ (firewall, NAT);
* плохо подходит для короткоживущих задач;
* нагрузка на систему мониторинга (она опрашивает всех).

---

## Задание 6:

> Какие из ниже перечисленных систем относятся к push модели, а какие к pull? А может есть гибридные?

### Ответ:

*Prometheus и Nagios* — в основе pull-модели.

*Zabbix и TICK* — гибридные системы, поддерживающие как push, так и pull.

*VictoriaMetrics* — storage-ориентированная система, которая поддерживает обе модели через интеграции. Система сама не реализует pull или push, но может принимать метрики от разных инструментов
